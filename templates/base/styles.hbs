{{#if _dev}}

  {{!-- fonts --}}
  <link href='http://fonts.googleapis.com/css?family=Lato:300,400,500,700,300italic,400italic,500italic,700italic' rel='stylesheet' type='text/css'>
  
  {{!-- own styles (compiled custom bootstrap) --}}
  {{!-- <link rel="stylesheet" href="/assets/css/style.css"> --}} {{!-- also injected now! --}}
  
  {{!-- vendor styles --}}
  {{#each _inject.css}}
    <link rel="stylesheet" href="{{this}}">
  {{/each}}
  
{{else}}

{{/if}}