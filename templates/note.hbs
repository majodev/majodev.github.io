{{> pre}}
<div id="wrapper">
  {{> header}}
  {{> noteBreadcrumb}}
  <div class="template-note">
    <main>
      <article>
        <div class="template-note-header">
          <div class="container-fluid"> 
            {{> noteNavigator}}
            {{> noteTitle}}
            {{> noteSubTitle}}
          </div>
        </div>
        <div class="template-note-contents">
          <div class="container-fluid">
            {{> contents}}
          </div>
        </div>
        <div class="template-note-footer">
          <div class="container-fluid">
            {{> noteSocial}}
          </div>
        </div>
      </article>
    </main>
  </div> {{!-- end template-note --}}
</div> {{!-- end wrapper --}}
{{> footer}}
{{> post}}