{{> pre}}
<div id="wrapper">
  {{> header}}
  <div class="template-note uiHandlerFade">
  {{!-- <div class="template-note pushdown-header uiHandlerFade"> --}}
    <main>
    <article>
      <header class="template-note-header-wrapper">
        <div class="template-note-header container-fluid">
          {{> noteNavigator}}
          {{> noteTitle}}
          {{> noteSubTitle}}
        </div>
      </header>
      <div class="template-note-contents container-fluid">
{{> contents}}
      </div>
      <footer class="template-note-footer container-fluid">
        {{> noteShare}}
        {{> noteComments}}
        {{> noteRelated}}
      </footer>
    </article>
    </main>
  </div> {{!-- end template-note --}}
</div> {{!-- end wrapper --}}
{{> footer}}
{{> post}}